# Backwards-compatible aliases for renamed filtering helpers

# Compatibility aliases for the former post-smoothing helper names
#
# These wrappers allow existing tests and package-internal calls to keep working
# while new code uses the shorter appendix-aligned names in
# `cp_uns_loc_filtering.R`.



