# Return the standard non-local result after a filter removes every cell

Return the standard non-local result after a filter removes every cell

## Usage

``` r
.getCpUnsLocEmptyFilterResult(
  dataMod,
  info,
  cpMin,
  exTblStimNoMin,
  exTblUnsBias
)
```
