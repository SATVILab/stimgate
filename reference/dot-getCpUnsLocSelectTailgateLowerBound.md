# Adjust and validate the tailgate floor against the marginal reference

Adjust and validate the tailgate floor against the marginal reference

## Usage

``` r
.getCpUnsLocSelectTailgateLowerBound(
  rawTailgateX,
  windowWidth,
  referenceX,
  adjustmentFraction = 1/4
)
```
