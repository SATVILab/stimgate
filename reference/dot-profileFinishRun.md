# Finish the run timer and collate the final profile

Finish the run timer and collate the final profile

## Usage

``` r
.profileFinishRun(
  pathProject = .profileState$pathProject,
  status = "completed"
)
```
