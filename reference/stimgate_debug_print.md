# Print the latest stimgate debug file to the console

Prints the most recent debug file created by stimgate to the console.

## Usage

``` r
stimgate_debug_print()
```

## Value

character The debug text (invisibly), or NULL if no debug file exists.
