# Read the first available setting from a precedence-ordered vector

Read the first available setting from a precedence-ordered vector

## Usage

``` r
.getCpUnsLocFirstSetting(chnlSettings, names, default = NULL)
```
