# Assert that object is a non-empty character vector

Assert that object is a non-empty character vector

## Usage

``` r
.assertStringVector(x)
```

## Arguments

- x:

  object Object to check

## Value

invisible NULL if check passes, otherwise throws an error
