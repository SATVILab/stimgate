# Check if object is a non-empty string

Check if object is a non-empty string

## Usage

``` r
.isString(x)
```

## Arguments

- x:

  object Object to check

## Value

logical TRUE if x is a character vector of length 1 with non-zero
characters
