# Locate x_deriv(alpha, omega, psi)

Locate x_deriv(alpha, omega, psi)

## Usage

``` r
.getCpUnsLocDerivThreshold(
  x,
  prob,
  deriv,
  alpha,
  omega,
  psi,
  thresholdProbMin = 0,
  capRightWidth = FALSE,
  leftRiseFrac = 0.15
)
```
