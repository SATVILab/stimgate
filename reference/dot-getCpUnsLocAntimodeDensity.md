# Fit the density used to identify antimodes

Fit the density used to identify antimodes

## Usage

``` r
.getCpUnsLocAntimodeDensity(
  expr,
  chnlSettings,
  originalBw = NULL,
  mtd = c("taut_string", "kde")
)
```
