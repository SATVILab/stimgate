# Attach shape-route diagnostics to the selected model data

Attach shape-route diagnostics to the selected model data

## Usage

``` r
.getCpUnsLocAttachShapeFitInfo(
  dataMod,
  shape,
  requested,
  applied,
  ordinaryDataMod
)
```
