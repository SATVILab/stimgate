# Check if object is a non-empty character vector

Check if object is a non-empty character vector

## Usage

``` r
.isStringVector(x)
```

## Arguments

- x:

  object Object to check

## Value

logical TRUE if x is a character vector of length \>= 1 with all
non-zero character elements
