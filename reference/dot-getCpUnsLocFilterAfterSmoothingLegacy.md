# Apply all filtering steps after smoothing

Apply all filtering steps after smoothing

## Usage

``` r
.getCpUnsLocFilterAfterSmoothingLegacy(
  dataMod,
  exTblStimNoMin,
  exTblUnsBias,
  cpMin,
  stage,
  chnlSettings
)
```
