# Time one expression and persist the completed timing record

Time one expression and persist the completed timing record

## Usage

``` r
.profileTime(
  expr,
  level,
  major,
  minor = NA_character_,
  operation = NA_character_,
  pathProject = NULL
)
```
