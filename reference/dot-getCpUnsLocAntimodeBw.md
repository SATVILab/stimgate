# Resolve the original fixed bandwidth used by the local-FDR densities

Resolve the original fixed bandwidth used by the local-FDR densities

## Usage

``` r
.getCpUnsLocAntimodeBw(expr, chnlSettings, originalBw = NULL)
```
