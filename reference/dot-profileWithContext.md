# Temporarily add profiling context

Temporarily add profiling context

## Usage

``` r
.profileWithContext(
  expr,
  marker = NULL,
  channel = NULL,
  batch = NULL,
  sample = NULL,
  stage = NULL
)
```
