test_that("analysis helper simulation tests are intentionally not part of the installed package suite", {
  skip("Analysis-side simulation helpers live in scripts/r and are not available in the built package tarball.")
})
